{
  "name": "ui",
  "root": "apps/ui",
  "sourceRoot": "apps/ui",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next build",
        "cwd": "apps/ui"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "next dev",
        "cwd": "apps/ui"
      }
    }
  }
}
